{
  "name": "weather-app-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Weather App with CRUD operations - PMA Bootcamp Technical Assessment",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["weather", "fullstack", "react", "express", "mongodb"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
